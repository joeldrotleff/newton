{
  "tasks": {
    "check": "deno check src/main.ts",
    "test": "deno test --allow-run --allow-read --allow-write --allow-env",
    "compile": "deno compile --allow-run --allow-read --allow-write --allow-env --output newton src/main.ts",
    "install": "deno task compile && mkdir -p ~/.local/bin && cp ./newton ~/.local/bin/newton",
    "uninstall": "rm -f ~/.local/bin/newton"
  },
  "fmt": {
    "lineWidth": 100,
    "semiColons": true,
    "singleQuote": false
  },
  "lint": {
    "rules": {
      "exclude": ["no-explicit-any"]
    }
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.10",
    "@std/path": "jsr:@std/path@^1.0.9"
  }
}
